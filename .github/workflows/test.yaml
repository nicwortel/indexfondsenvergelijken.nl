name: test

on: push

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@master
      - name: Install NPM packages
        run: npm install
      - name: Run tests
        run: npm test
